[Service]
Environment="http_proxy={{ proxy.http_proxy }}"
Environment="https_proxy={{ proxy.https_proxy }}"
Environment="no_proxy={{ proxy.no_proxy }}"
Environment="HTTP_PROXY={{ proxy.http_proxy }}"
Environment="HTTPS_PROXY={{ proxy.https_proxy }}"
Environment="NO_PROXY={{ proxy.no_proxy }}"
Environment="CONTAINERD_HTTP_PROXY={{ proxy.http_proxy }}"
Environment="CONTAINERD_HTTPS_PROXY={{ proxy.https_proxy }}"
Environment="CONTAINERD_NO_PROXY={{ proxy.no_proxy }}"
